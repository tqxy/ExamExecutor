# 高精度OCR处理模块依赖包

# 基础依赖
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.3.0

# Paddle OCR依赖 (通用文本识别)
paddleocr>=2.0.0
paddlepaddle>=2.0.0

# 日志和工具
# logging>=0.4.9.6 (这是Python标准库，不需要安装)

# 如果使用GPU加速，取消注释下面的行
# paddlepaddle-gpu>=2.0.0

# 开发依赖
pytest>=6.2.0
flake8>=3.9.0